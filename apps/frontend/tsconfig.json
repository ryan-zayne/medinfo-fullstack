{
	"extends": ["../../tsconfig.base.json", "@zayne-labs/tsconfig/bundler/next"],

	"compilerOptions": {
		"paths": {
			"@/*": ["./*"]
		}
	},

	"include": ["**/*.ts", "content/**/*.mdx", "**/*.tsx", ".next/dev/types/**/*.ts"],
	"exclude": ["node_modules"]
}
