// Avoid importing from ./env to prevent dragging in backend-only dependencies (consola) into email previews
// const NODE_ENV = process.env.NODE_ENV ?? "development";

export const FRONTEND_URL = "https://medical-info.vercel.app";
